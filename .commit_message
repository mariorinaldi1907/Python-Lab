added 3 new scripts

- built a recursive descent parser for math expressions with proper operator precedence
- built an async rate limiter with token bucket algorithm, works great for API call throttling
- implemented bidirectional BFS for shortest paths - way faster than regular BFS on large graphs