added 3 new scripts

- built a word frequency analyzer with CLI, supports multiple files and filtering by min occurrence count
- built an SIR epidemic simulator with configurable params, prints daily stats and visualizes spread over time
- built a modular arithmetic toolkit with fast exponentiation, extended gcd, and multiplicative inverses