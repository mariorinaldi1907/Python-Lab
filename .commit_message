added 3 new scripts

- implemented convex hull using gift wrapping algorithm, added visualization with ASCII art because why not
- built a CLI log analyzer that parses logs by level and timestamp, supports filtering and stats
- built a simple SIR epidemic model to mess around with disease spread dynamics