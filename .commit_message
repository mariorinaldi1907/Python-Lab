added 2 new scripts

- built a lazy pipeline utility with chaining and memoization — feels like a mini lodash but pythonic
- built an LRU cache from scratch using a hash map + doubly linked list combo