added 3 new scripts

- built a word frequency analyzer that handles stopwords and outputs pretty tables — added sorting options too
- implemented gift wrapping algorithm for 2D convex hulls, handles collinear points properly
- built a multithreaded port scanner with timeout handling, ranges work nicely