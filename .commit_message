added 2 new scripts

- built an nginx access log parser that extracts metrics and detects suspicious patterns
- built a Markov chain text generator that actually produces readable output, tweaked state size for better coherence