added 2 new scripts

- built a modular arithmetic toolkit with fast exponentiation, inverse, and CRT solver
- built an async rate limiter with token bucket algorithm, handles bursts and backpressure nicely