added 3 new scripts

- implemented patience diff algorithm - cleaner diffs for code than traditional myers, handles unique lines better
- implemented k-means clustering from scratch, figured out the centroid update logic and added early stopping
- implemented an LRU cache from scratch using a doubly-linked list and hashmap — wanted to understand how functools.lru_cache works under the hood