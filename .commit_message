added 2 new scripts

- built a recursive descent parser for basic math expressions, handles precedence and parentheses correctly
- built a word frequency analyzer CLI that handles stopwords and outputs sorted results — useful for quick text analysis