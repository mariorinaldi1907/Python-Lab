added 3 new scripts

- implemented word ladder solver with bidirectional BFS, way faster than I expected on large dictionaries
- built a naive bayes classifier from scratch, trained it on iris-like data and got decent accuracy
- built a modular arithmetic toolkit with fast exponentiation, modular inverse, and CRT solver — pretty useful for cryptography stuff