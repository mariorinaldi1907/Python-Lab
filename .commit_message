added 3 new scripts

- built an async rate limiter with token bucket algorithm, tested with simulated API calls
- built a bayesian A/B test analyzer because I got tired of guessing if my changes actually mattered
- implemented command pattern for a text editor with undo/redo — feels clean for action history