added 2 new scripts

- built an async rate limiter with token bucket algorithm, handles burst traffic nicely
- built a markov chain text generator with configurable n-gram order, been wanting to make one of these for a while