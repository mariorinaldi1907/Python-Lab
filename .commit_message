added 3 new scripts

- built a nginx access log parser that actually handles the messy real-world formats i keep seeing
- implemented BFS word ladder solver, finds shortest path between two words by changing one letter at a time
- implemented Tarjan's SCC algorithm because I was curious how cycle detection works in directed graphs