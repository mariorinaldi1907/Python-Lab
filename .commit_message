added 2 new scripts

- built a huffman encoder/decoder from scratch, actually saves space on repeated text
- built a recursive descent parser for arithmetic expressions, handles precedence and parentheses cleanly