added 3 new scripts

- implemented topological sort with cycle detection using DFS, returns None if graph has cycles
- built an async rate limiter with token bucket algorithm, actually pretty clean implementation
- built an LRU cache from scratch using a dict + doubly linked list, handles capacity eviction cleanly