added 2 new scripts

- built a recursive JSON schema validator that checks types, required fields, and nested objects
- built a Bayesian A/B test analyzer with conjugate priors, way cleaner than frequentist p-value hell