added 2 new scripts

- built a CLI deduplicator that hashes files to find exact duplicates, added size pre-filtering to avoid hashing everything
- implemented bootstrap resampling for confidence intervals, threw in some basic stats helpers too