added 2 new scripts

- built a recursive descent parser for basic math expressions, handles precedence and parentheses properly
- built a multithreaded port scanner that actually respects timeouts and doesn't hang forever