added 3 new scripts

- built a lazy evaluation pipeline for chaining operations without intermediate lists — saves memory on large datasets
- implemented word ladder solver with BFS, throws in some nice path reconstruction
- implemented LRU cache with OrderedDict, pretty happy with how clean the eviction logic turned out