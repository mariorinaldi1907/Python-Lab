added 3 new scripts

- built a Bayesian A/B testing module with beta distributions, way cleaner than frequentist p-values
- implemented Pollard's rho algorithm for integer factorization with trial division fallback
- built a threaded port scanner that actually respects timeouts and doesn't hang forever