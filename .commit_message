added 2 new scripts

- implemented Pollard's rho factorization with Miller-Rabin primality testing, handles large composites efficiently
- built a simple port scanner with basic service detection - checks common ports and tries to identify what's running