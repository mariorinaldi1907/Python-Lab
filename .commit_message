added 2 new scripts

- built a reverse polish notation calculator with operator extensibility and error handling
- built a word ladder solver using BFS — finds shortest path between words by changing one letter at a time