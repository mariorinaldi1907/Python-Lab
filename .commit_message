added 2 new scripts

- built a markov chain text generator that actually works pretty well with variable n-gram sizes
- implemented word ladder solver with BFS, finds shortest transformation path between any two words