added 2 new scripts

- built a bayesian A/B test analyzer with beta distributions, way cleaner than frequentist p-values
- built a concurrent port scanner with threading — scans common ports way faster than sequential