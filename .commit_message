added 2 new scripts

- built a huffman encoder/decoder from scratch, handles arbitrary text and shows the compression ratio
- built an apache/nginx log parser that extracts metrics and detects suspicious patterns