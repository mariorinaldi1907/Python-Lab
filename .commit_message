added 3 new scripts

- built a recursive descent parser for basic math expressions, handles precedence and parentheses correctly
- implemented run-length encoding with decode support, handles edge cases like single chars and empty strings
- built a bayesian A/B test analyzer because I'm tired of waiting for "statistical significance"