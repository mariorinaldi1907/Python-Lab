added 2 new scripts

- implemented topological sort using Kahn's algorithm, added cycle detection because dealing with circular dependencies sucks
- built a multi-threaded TCP port scanner with timeout handling and service detection