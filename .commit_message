added 2 new scripts

- built a lazy evaluation pipeline with memoization support — chains transformations without eager computation
- built a huffman encoder from scratch, compresses text pretty well and shows the actual tree structure