added 2 new scripts

- built naive bayes classifier from scratch, tested on iris-like data with laplace smoothing
- built an nginx access log parser that extracts useful stats like top IPs, status codes, and request patterns