added 2 new scripts

- built a markov chain text generator with configurable chain order, handles punctuation and generates pretty natural-sounding nonsense
- built a lazy pipeline system with deferred execution and infinite sequence support