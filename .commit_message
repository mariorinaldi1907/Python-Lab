added 2 new scripts

- built an async rate limiter with token bucket algorithm, handles burst traffic smoothly
- implemented Graham scan for convex hull because I needed it for a collision detection prototype