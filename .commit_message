added 3 new scripts

- implemented word ladder solver with BFS, found shortest path between two words by changing one letter at a time
- implemented LRU cache with doubly linked list and hashmap, O(1) get/put operations
- built a word frequency CLI that tokenizes text files and ranks words by occurrence, added min-count filtering and case-insensitive mode