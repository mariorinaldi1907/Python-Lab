added 3 new scripts

- built a Huffman encoder/decoder from scratch, handles arbitrary text and shows compression stats
- built an asyncio rate limiter with token bucket algorithm, handles burst traffic nicely
- implemented KMP string matching because I was tired of writing O(n*m) brute force searches