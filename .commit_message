added 2 new scripts

- built a word ladder solver using BFS — finds shortest path between two words by changing one letter at a time
- built a bootstrapping module with confidence intervals and hypothesis testing — handles both numeric and categorical data