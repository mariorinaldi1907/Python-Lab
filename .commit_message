added 2 new scripts

- implemented Graham scan for convex hull, handles collinear points and edge cases properly
- built a lazy pipeline for chaining transformations without executing until needed, pretty happy with how composable it turned out