added 3 new scripts

- implemented gift wrapping algorithm for convex hulls, threw in some visualization with ascii art for fun
- implemented bidirectional BFS for shortest path — way faster on large graphs than vanilla BFS
- built a lazy pipeline system with memoization — finally understand why functional programming nerds won't shut up about this