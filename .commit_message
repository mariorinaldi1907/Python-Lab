added 2 new scripts

- implemented word ladder solver with BFS, tracks path and handles edge cases like unreachable targets
- built an LRU cache from scratch using a doubly linked list and dict for O(1) operations