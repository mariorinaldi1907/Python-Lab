added 3 new scripts

- built a modular arithmetic toolkit with fast exponentiation and CRT solver — needed this for some cryptography practice
- built an nginx access log parser that extracts stats and handles malformed lines gracefully
- built an async rate limiter with token bucket algorithm, supports burst traffic and multiple concurrent workers