added 2 new scripts

- built a huffman encoder from scratch, actually compresses text pretty well
- added bootstrap resampling for confidence intervals, handles both mean and median with percentile method